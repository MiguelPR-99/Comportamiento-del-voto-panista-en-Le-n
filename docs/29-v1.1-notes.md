# v1.1 Notes - Pulido visual/editorial

## Cambios realizados

- Leyenda:
- Se reemplazaron labels tecnicos visibles (`growth_mid`, `stable_high`, etc.) por etiquetas editoriales en espanol natural.
- Se mantuvieron intactas las clases internas y el dominio bivariante.
- Inset:
- Se retiro copy tecnico visible.
- Se dejo nota editorial discreta: "Inset urbano: acercamiento al area central de Leon."
- Encuadre visual:
- Se ajusto el `fitBounds` del mapa principal para reducir aire excesivo (padding mas compacto).
- Copy visible:
- Se normalizaron textos visibles en frontend (Seccion, Variacion, Exportacion, Creditos, Fuentes, Sin datos suficientes).
- Limpieza ligera:
- Se extrajeron labels/formateadores a `lib/presentation.ts`.
- `MapShell` quedo mas legible sin cambiar comportamiento funcional de datos.

## Que no cambio

- No se modifico `data/raw`.
- No se modifico `data/interim`.
- No se modifico `public/data/secciones.geojson`.
- No se cambiaron metricas, bins, clasificacion ni metodologia.
- No se agregaron filtros ni patron dashboard.
- No se hicieron refactors estructurales grandes.

## QA ejecutado

- `npm run lint`: OK
- `npm run build`: OK (en entorno sin restriccion de sandbox; en sandbox aparecio `spawn EPERM`)
- `npm run export:png`: OK (export final generado en `exports/leon_pan_bivariate_desktop.png`)
- Revision visual de export y carga local:
- mapa principal visible
- inset visible
- leyenda visible
- tooltip sin cambios funcionales (componente no alterado en logica de datos)

## Pendientes futuros (no bloqueantes)

- Afinar microtipografia de leyenda en desktop/mobile.
- Definir geometria formal de inset para siguiente iteracion.
- Profundizar accesibilidad avanzada (AA) y pruebas visuales automatizadas.
